# 项目解析：laidongzhen/blog

这份文档说明当前仓库「文档写在哪、站点发出去什么、哪些只是资料堆」，以及在**不推倒重来**的前提下，怎样兼顾现状和以后加分类、加主题、双仓发布。

线上站点：[https://laidongzhen.github.io/blog/](https://laidongzhen.github.io/blog/)  
技术栈：VitePress `1.1.4` + GitHub Actions + GitHub Pages（`base: /blog/`）。  
文档根目录：`docs/`（见 [`.vitepress/config.mts`](.vitepress/config.mts) 的 `srcDir`）。

---

## 1. 一句话

这是个人技术笔记站，不是产品文档站。写作方式是 Markdown + 手写顶栏/侧边栏。「考试 / 前端 / 专题」已并入 `docs/notes/`，顶栏只留首页、笔记、友情链接。旧路径跳转页、草稿目录、以及仓库里的 `example/`、`information/` 资料树已删除，本仓只保留站点配置和可发布文档。

---

## 2. 仓库现在两层

| 层 | 路径 | 是否进站点 | 现在干什么 |
| --- | --- | --- | --- |
| 站点源码 | `.vitepress/`、`package.json`、`.github/workflows/` | 构建用 | 主题、导航、CI |
| 对外文档 | `docs/` 下几乎所有 `.md` | **会**（VitePress 默认全量编译） | 读者能打开的页面 |

`example/`、`information/` 已从本仓删除。若仍需要课程代码或手写示例，应放在独立仓库，用链接引用，不要再塞回博客仓。

---

## 3. `docs/` 文件夹与文档对应关系

当前顶栏三条，磁盘上正式目录与之对齐：`docs/index.md`、`docs/notes/`、`docs/friends/`。

### 3.1 导航能走到的（正式面）

```
docs/
  index.md                 首页（开始阅读 → /notes/）
  friends/index.md         友情链接（MDN、Vue、面试站等）
  notes/
    index.md               笔记总索引
    前端路线规划.md
    项目经历/              QMS、jeecg、访客小程序
    Vue/                   Vue3 语法、源码笔记
    工具与流程/            Gitee-GitHub 镜像、VSCode、软考、本地 WordPress
    项目问题/              Element 表单/表格等问题
    杂/
    exam/                  原「考试」整目录迁入
    frontend/              原「前端」整目录迁入（HTML/CSS/JS/Vue/React）
    topic/                 原「专题」整目录迁入（VitePress / ElementUI / Vue问题 / Vant）
```

侧边栏配置在 [`.vitepress/sidebar.mts`](.vitepress/sidebar.mts)：现有笔记分组之后接「考试 → 前端 → 专题」。**新增一篇文章必须改这个文件**，目录和侧边栏不会自动同步。

### 3.2 已删除（不再保留旧路径）

顶层 `docs/exam`、`docs/frontend`、`docs/topic` 跳转页，以及 `暂`、`笔记草稿`、`技术`、`notes/moduleA`、`notes/moduleB` 已删除。旧线上地址（如 `/blog/frontend/`）会 404，请改用 `/blog/notes/frontend/` 等新路径。

### 3.3 仍会被编译、但侧边栏未挂的文件

| 路径 | 说明 |
| --- | --- |
| `docs/notes/选题草稿箱.md`、`前端路线规划 - 当前进度版.md` | 未挂侧边栏，双份路线规划 |
| `docs/notes/项目问题/jeecg框架开发说明.md` | 有文件、侧边栏未登记 |
| `docs/README.md` | 仓库说明，会被编成站点页 |

---

## 4. 内容盘点（按主题，不论文件夹名字）

把「文件夹名」和「文章实际在讲什么」对齐之后，目前大致是六类：

1. **项目档案**：QMS 三件套、jeecg、访客小程序（`notes/项目经历/`）。这是最有个人壁垒的内容。
2. **踩坑手册**：Element / Vant / Vue2 API（`项目问题` + `topic/` 里同题重复）。
3. **体系学习**：HTML/CSS/JS 索引页偏空；Vue 有 coderwhy 课堂笔记；React 有入门/工具/原理。
4. **站点工程**：VitePress + GitHub Actions 搭建文、仓库镜像。
5. **考试**：信管第一章；「高级软考」却放在 `工具与流程/`，和 `exam/` 分类打架。
6. **碎片**：写博客注意点、选题箱、WordPress 本地站。

重复需要心里有数（本轮按计划不去重，但扩展前必须处理）：

- Element 表格/多选等：`notes/项目问题/` 与 `notes/topic/ElementUI问题/` 各一份。
- Vue：`notes/Vue/`（自己的语法/源码）与 `notes/frontend/vue/coderwhy的vue3/`（课堂）主题近、路径两套。
- 课堂稿只保留 `notes/frontend/vue/coderwhy的vue3/`。

---

## 5. 配置与工程现状（影响以后能不能加功能）

- **分类全靠手写 nav/sidebar。** 文章一多，侧边栏会变成一屏「全部展开」（现在 `collapsed: false`）。以后加「规范 / 必知必会 / 后端」若仍全部塞进 `/notes`，导航会先崩。
- **中文路径做 URL。** 利于本地 Typora，不利于外链稳定和跨系统；Git 在 Windows 上对中文路径也更脆。
- **`lang: 'en-US'`**，正文是中文；搜索文案配在 `locales.zh` 上，和站点语言对不齐。
- **工作流文件名** `.github/workflows/deply.yml` 拼写错误，不影响跑，但不利于后人检索。
- **图片约定**见 `docs/notes/杂/写博客文章注意点.md`：跟文同名目录 `./${filename}`。迁目录后相对路径一般还在，但旧跳转页没有带走图片。
- 友情链接里仍有 `example.com` 占位，对外观感是半成品。

---

## 6. 对这个「技术文档记录项目」的建议

原则：**站点（可发布 Markdown）和资料（课程/源码）分离；笔记里再分「碎片」和「成体系」**。顶栏可以暂时少，磁盘分类要先想好，否则下次加分类还是搬家。

### 近期（保持现有顶栏，把仓收拾干净）

1. **更新 [docs/README.md](docs/README.md)**，写成现在的真实地图（notes 下含 exam/frontend/topic，顶层 exam/frontend/topic 只是跳转）。
2. **侧边栏漏挂**：选题箱、第二份前端路线规划、`jeecg框架开发说明.md` 要么挂上要么移出 `docs/`。
3. **侧边栏与磁盘对齐**：漏挂的 `jeecg框架开发说明.md`、两份前端路线规划，要么挂上要么移出 `docs/`。
4. **去重只做登记，先不删文件**：同一 Element 问题在侧边栏只留一处入口（另一处文内写「见 xxx」）。Vue 课堂笔记归 `frontend/vue`，自己的源码笔记留 `notes/Vue`，职责写进 README。
5. **友情链接去掉占位 URL**，或标明「待补」。
6. **`lang` 改为 `zh-Hans`**，搜索 locale 与之一致。

### 中期（为扩展分类做结构，而不是先改顶栏）

以后若按小册加「技术规范 / 必知必会 / 前端 / 后端 / 深入 / 项目 / 其它」，**不要再从零建站**，在现仓加目录即可。为避免再来一次大搬家，建议磁盘上**现在就按「以后的顶栏」分子目录**，顶栏可以仍只显示「笔记」一个入口（内部用侧边栏分组），或笔记里用二级分组名对齐未来 nav。

推荐目标形态（文件可以逐步挪，URL 用跳转页保住旧链）：

```
docs/
  notes/           碎片、未归类、每日坑（inbox）
  frontend/        成体系前端（从 notes/frontend 再升回顶层也可以，但要一次性、配 redirect）
  projects/        现 notes/项目经历
  exam/            考试（软考从「工具与流程」挪过来）
  cookbook/        现 项目问题 + topic 里的坑
  friends/
```

在升顶栏之前，保持「一个主题只有一个家」。inbox（`notes/`）只收尚未分类的短文，避免再变成全能抽屉。

侧边栏中期改为 **按目录生成**（脚本扫 `docs/**/index.md` 或 frontmatter），减少漏登记。新文章约定 frontmatter：`title`、`date`、`category`、`tags`，以后做列表页、按标签检索才有数据。

路径中期逐步改成英文 slug（`frontend/vue/options-api.md`），中文只做标题；旧中文 URL 继续留跳转。这是扩展多分类、多语言、外链时最省事的一步。

### 远期（站点产品能力，仍免费）

- 主题：CSS 变量换肤即可，不必换生成器。
- 发布：GitHub Pages 为主；Gitee 只镜像源码（Pages 已不可用）；国内访问再考虑 Cloudflare Pages。
- 搜索：本地搜索够用；文章过千再考虑 Algolia/Pagefind。
- 评论、浏览量都不是现在的瓶颈；瓶颈是**侧边栏手写**和分类尚未定型。

---

## 7. 建议的落地顺序（可执行）

1. 资料仓、代码仓若仍需要，用独立仓库存放，本仓只留链接。
2. 信息架构：去重入口 + 软考归考试；漏挂的 md 挂上或移出。
3. 约定：新文英文目录、frontmatter、先写 notes inbox 再提升到专题目录。
4. 等分类稳定后再改顶栏（小册式多栏目）；若改 URL，再决定是否加跳转。

---

## 8. 和「解析代码页面」的区别

本仓库几乎没有业务 Vue 页面，主体是静态文档。上面分析的是**文档信息架构和仓库职责**，不是某个后台 CRUD 页的交互流程。站点行为入口就是：

- 顶栏：[`.vitepress/nav.mts`](.vitepress/nav.mts)
- 侧边栏：[`.vitepress/sidebar.mts`](.vitepress/sidebar.mts)
- 首页：[docs/index.md](docs/index.md)
- 笔记索引：[docs/notes/index.md](docs/notes/index.md)

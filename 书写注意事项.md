# 书写注意事项

本文件是博客写作约定的**唯一说明**，放在仓库根目录，不进站点。以后有新坑、新约定，直接改这一篇并在文末「修订记录」加一行。

站点文档都在 `docs/` 下。顶栏和侧边栏由 [`.vitepress/scan-docs.mts`](.vitepress/scan-docs.mts) 按目录自动生成，**一般不用改** `nav.mts` / `sidebar.mts`。

---

## 1. 新增一篇文章

1. 把 `.md` 放到对应分类目录。个人笔记放 `docs/notes/...`；项目规范放 `docs/handbook/规范/`；页面解析放 `docs/handbook/页面解析/`。
2. 重启一次 `npm run docs:dev`（或保存一下 `.vitepress` 里的配置），菜单才会重扫目录。
3. 页面路由等于文件路径（再加站点前缀 `/blog/`），例如上面那篇是 `/blog/notes/项目问题/el-table滚动条`。

新开顶栏栏目：在 `docs/` 下建一级文件夹，并放 `index.md`（写 `# 栏目名`）。例如 `docs/backend/index.md`。

不想出现在菜单、但仍可打开 URL：

- 文件名以 `_` 开头，如 `_草稿.md`，或
- 文头加 `hidden: true`（见下一节）

---

## 2. 排序（order）

同一层里，数字**越小越靠前**。没写 `order` 的当作无穷大，再按文件名中文排序。

**顶栏：** 写在该栏目的 `index.md` 上。现在约定：

| 栏目 | 文件 | order |
| --- | --- | --- |
| 笔记 | `docs/notes/index.md` | 1 |
| 项目手册 | `docs/handbook/index.md` | 50 |
| 前端导航链接 | `docs/friends/index.md` | 99（保证在最后） |

以后要在现有栏目之间再插一项，给新栏目 `index.md` 写介于两者之间的 `order` 即可。

**侧栏（同一文件夹内的文章或子目录）：** 写在该篇文章，或子目录的 `index.md` 上。

```md
---
title: 侧栏上显示的名字
order: 10
hidden: false
---

# 文章标题
```

`title`：菜单文案。不写则用文中第一个 `# 标题`，再没有就用文件名。  
`order`：只影响**同一层**的先后，不会让文章跑到别的文件夹去。  
`hidden: true`：不进顶栏/侧栏，页面仍可访问。  
`collapsed: false`：只对**有子项的分组**（子目录的 `index.md`）生效，侧栏默认展开；不写则默认收起。单篇文章不要写这个字段，章节目录看右侧「On this page」即可。

示例：希望「前端路线规划」排在笔记侧栏最前，在 `docs/notes/前端路线规划.md` 加：

```md
---
title: 前端路线规划
order: 1
---
```

希望「仓库镜像」排在「工具与流程」分组最前，在该篇加 `order: 1`，同目录其它文 `order: 2`、`3`…

改完 `order` 后同样需要**重启 dev** 才会看到新顺序。

---

## 3. 文件和目录怎么命名

可以正常用，但有的符号会让 Windows、Git、GitHub Pages 出问题。

**建议用**

- 中文或英文、数字
- 连字符 `-`（没问题，英文文件名首选：`el-table-scroll.md`）
- 下划线 `_`（不要放在文件名**开头**，开头表示不进菜单）

**尽量不用**

- 顿号 `、`、逗号 `,`、斜杠 `/ \`、竖线 `|`
- 英文冒号 `:`、星号 `*`、问号 `?`、引号 `" '`、尖括号 `< >`
- 井号 `#`、百分号 `%`、加号 `+`、空格（URL 会变成 `%20`，本地有时正常、线上偶发 404）
- 文件名里再套一层扩展名，如 `说明.md.bak.md`

顿号 `、` 在部分系统能打开，但 Git 状态、克隆到 Linux CI 时编码不稳定，**不要用**。  
书名号、括号可以，但侧栏和 URL 都会变长，能省则省。

文件夹名会进 URL，同样遵守上面规则。一篇文章一个文件夹放图片时，文件夹名必须和 **md 主文件名（不含 `.md`）完全一致**。

---

## 4. 图片：同目录文件夹 + 相对路径

约定：**图片不进 `public/`，不写网图 URL。** 每篇文章旁边建**同名文件夹**，图片都放进去。这样本地 Typora / VS Code 能预览，VitePress 构建会打包进站点，GitHub Pages 也能打开。

```
docs/notes/项目问题/el-table滚动条.md
docs/notes/项目问题/el-table滚动条/demo.png
```

Markdown 里用**相对路径**（不要 `/blog/...`，不要 `http://`）：

```md
![滚动条问题截图](el-table滚动条/demo.png)
```

对应关系：`(文件夹名/文件名)`，文件夹名 = 当前 md 去掉 `.md`。

### Typora

「偏好设置 → 图像」：

- 插入图片时：复制到指定路径
- 路径填：`./${filename}`

粘贴截图会自动进「当前文件名」文件夹，引用也是相对路径，本地和远程一致。

### VS Code / Cursor

手动建同名文件夹，把图放进去，再按上面的语法引用。不要用 Markdown 预览生成的 `file:///` 绝对路径。

### 清晰又尽量快

- 界面截图用 **PNG**；照片、大图用 **WebP** 或压缩过的 JPG。
- 宽度够看即可，一般不超过 **1920px**。4K 原图又大又慢，放大也看不出字。
- 单张尽量控制在 **500KB 以内**，实在清楚需要可以到 1MB。超过 2MB 要想办法压一下。
- **不要**用外链图床（会失效、国内加载慢）。**不要**把图只放在本地盘却不提交 Git。
- 引用必须指向仓库里真实存在的文件，否则构建会报找不到资源。

提交时把 `.md` 和旁边的图片文件夹**一起** `git add`。

### 点击放大

正文里的图点击后会全屏灯箱查看（盖住顶栏和侧栏），滚轮可继续局部放大，拖动可平移。写作时不用加特殊语法，保证相对路径正确即可。点空白、点关闭或按 Esc 退出。

---

## 5. 正文写法

1. 标点尽量用英文符号，尤其是代码、路径、frontmatter。中文叙述可以用中文标点。
2. `watch`、`computed`、`data` 这类词在正文里要加反引号，写成 \`watch\`，否则可能被当成 Vue 编译。
3. 文首只保留**一个**一级标题 `#`。菜单标题优先用 frontmatter 的 `title`。
4. 代码块标明语言，如 \`\`\`ts 、 \`\`\`vue 。
5. 不要用 HTML 注释包一整篇（个别文件头部的 Author 注释可以保留）。
6. 分类目录要有 `index.md`，作为这个分组的落地页，侧栏才有分组名和链接。
7. 草稿、进度版、重复稿加 `hidden: true`，或文件名 `_` 开头，不要和正式文混在菜单里。

### 侧栏在、正文空白

VitePress 会把 Markdown **当成 Vue 模板编译**。侧栏、顶栏、页脚「上一篇 / 下一篇」来自主题，不受影响；中间正文是当前这篇编译出来的组件，编译失败就会整片空白。

常见触发（表格、行内反引号、普通正文都会中招；**三反引号围栏代码块**一般安全，校验也会跳过）：

- 写成 HTML 标签的 `script` / `style` / `template`（含 `script setup`）。编译器当成真标签，缺结束标签或抽成 SFC 块后，后面的正文被吃掉或整页编译失败。
- 写成 `{{ user.name }}` 这种双花括号。编译器当成 Vue 插值，变量不存在就会渲染失败。

需要提到这些词时：写成「script setup」「style 块」「template」，或把标签拆开写，不要直接打出完整尖括号标签和 `{{ }}`。

预览时若只有中间空白、菜单却是对的，先在这篇 md 里搜 `script`、`style`、`template`、`{{`。提交前校验见下一节「用 Git 提交」。

frontmatter 完整示例：

```md
---
title: el-table 固定列滚动条无法拖动
order: 20
hidden: false
---

# el-table 固定列滚动条无法拖动

正文……

![现象](el-table固定列滚动条无法拖动/demo.png)
```

（若文件名是 `el-table固定列滚动条无法拖动.md`，图片文件夹也叫这个名字。）

---

## 6. 本地预览和发布

```bash
pnpm run docs:dev
```

浏览器打开提示的地址（带 `/blog/`）。确认：侧栏顺序、图片能显示、点击能全屏放大。

推到 `main` 后 GitHub Actions 会构建并发布到 GitHub Pages。相对路径的图会打进 `dist`，一般无需额外配置。

改了 `order` / 新增 md / 新增图片文件夹后若菜单或图没更新：停掉再执行 `pnpm run docs:dev`。

---

## 7. 用 Git 提交（会跑文档校验）

校验挂在 **husky 的 pre-commit** 上：只要执行的是真正的 `git commit`（命令行、Cursor / VS Code 源代码管理、其它 Git 客户端），都会先跑校验。**不是**只有命令行才校验；IDE 点提交一般也会走同一套钩子。

校验内容：只检查**已暂存**的 `docs/**/*.md`，有没有裸写的 `script` / `style` / `template` 标签和 `{{ }}`（围栏代码块内跳过）。不通过则提交失败，终端会打印行号和改法。

克隆仓库后先装依赖，钩子才会生效：

```bash
pnpm install
```

（`prepare` 会执行 `husky`，配置 `core.hooksPath`。）

### 推荐命令行步骤

在仓库根目录 `C:\myworkspace\blog`：

```bash
# 1. 看改了哪些文件
git status

# 2. 只把要提交的文件加入暂存区（不要盲目 git add .）
git add .
git add docs/handbook/规范/某篇.md
# 有配图时把同名文件夹一起加：
# git add docs/handbook/规范/某篇.md docs/handbook/规范/某篇/

# 3.（可选）提交前先手动校验暂存的 md
pnpm run docs:check

# 4. 提交：这里会自动跑 pre-commit → docs:check
git commit -m "docs: 补充某篇说明"

# 5. 确认提交成功
git status

# 6. 需要发布时再推远程（推 main 会触发 GitHub Pages）
git push
```

一次加多类改动也可以，例如：

```bash
git add docs/ .vitepress/ scripts/ package.json pnpm-lock.yaml 书写注意事项.md .husky/ .cursor/skills/
git commit -m "feat: 项目手册、图片灯箱与文档提交校验"
```

### 校验相关命令

| 命令 | 作用 |
| --- | --- |
| `pnpm run docs:check` | 校验**暂存区**里的 docs md（和 pre-commit 相同） |
| `pnpm run docs:check:all` | 校验 `docs/` 下全部 md（历史文可能有存量问题） |
| `node scripts/check-docs.mjs docs/某路径/某篇.md` | 只查指定文件 |

### 注意

- **没 `git add` 的文件不会进校验，也不会进这次提交。**
- 不要用 `git commit --no-verify` 跳过钩子，除非你明确知道在绕过什么。
- 校验失败：按提示改 md → 再 `git add` 改过的文件 → 重新 `git commit`（不要 amend，除非你很清楚在改刚打的未推送提交）。
- Cursor / VS Code 左侧源代码管理里勾选文件再填说明提交，一般也会触发同一 pre-commit；若从未 `pnpm install`，钩子可能没装上。

---

## 修订记录

以后改约定请在这里追加一行，方便对照。

| 日期 | 说明 |
| --- | --- |
| 2026-08-28 | 初版：order 排序、同目录图片、命名限制、点击放大、frontmatter |
| 2026-08-28 | 图片改为全屏灯箱查看，支持滚轮局部放大（viewerjs） |
| 2026-08-28 | 新增顶栏「项目手册」：规范放 `docs/handbook/规范/`，页面解析放 `docs/handbook/页面解析/` |
| 2026-08-28 | 正文避免裸写 script/style/template 标签和 `{{ }}`，否则页面空白 |
| 2026-08-28 | 补充「侧栏在、正文空白」原因：VitePress 把 md 当 Vue 模板编译 |
| 2026-08-28 | 提交前校验暂存 md：`pnpm run docs:check`（scripts/check-docs.mjs） |
| 2026-08-28 | 去掉考试栏目、React、html/css 空目录、草稿箱/进度版/杂、jeecg 废稿 |
| 2026-08-28 | 前端路线规划 `order: 1` 排侧栏最前；`collapsed` 仅用于分组目录 |
| 2026-08-28 | 补充「用 Git 提交」：命令行步骤与 pre-commit 说明 |